{
  "name": "rack",
  "private": true,
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "dev": "turbo run dev",
    "check:styles": "node scripts/check-design-tokens.mjs",
    "build": "pnpm check:styles && turbo run build",
    "typecheck": "pnpm check:styles && turbo run typecheck",
    "preview": "turbo run preview"
  },
  "devDependencies": {
    "turbo": "2.5.6"
  }
}
