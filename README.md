# Rack

A pnpm + Turbo TypeScript monorepo starter with a shared config package and one React app.

## Structure

- `apps/web` - Vite + React + Tailwind app
- `packages/config` - shared repo config for TypeScript and Tailwind

## Commands

- `pnpm dev`
- `pnpm build`
- `pnpm typecheck`

