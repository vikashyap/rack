import type { RackTemplateProps } from "@repo/config";

import { RackBaseTemplate } from "./RackBaseTemplate";

export function Rack42Template(props: RackTemplateProps) {
  return <RackBaseTemplate {...props} />;
}
